# Levy Games — הנחיות פיתוח

פורטל משחקים משפחתי בעברית (RTL). **הפורטל עצמו ורוב המשחקים סטטיים טהורים:**
אין build, אין תלויות, אין backend, אין DB. הקוד מוגש כמו שהוא.

משחק שכן דורש build (React/Vite) מקבל טיפול נפרד — ראו "משחק עם build" למטה.

---

## חוק ראשון: אין נגיעה ישירה ב-localStorage

לאף קובץ חדש אסור לקרוא ל-`localStorage` ישירות. **כל** שמירה עוברת דרך
שכבת האחסון, כי היא זו שמתייגת את המידע לפרופיל של מי שמשחקת.

```
js/local-store.js   הדרייבר היחיד שנוגע ב-localStorage. נקודת ההחלפה לעתיד.
js/profiles.js      רג'יסטרי הפרופילים (מי משחקת עכשיו)
js/storage.js       ה-API שכולם עובדים מולו
```

### שני משטחי גישה — לפי סוג הסקריפט

| סוג הקוד | איך ניגשים |
|---|---|
| `<script type="module">` | `import { read, write } from '../../js/storage.js?v=1'` |
| `<script>` קלאסי / bundle | דרך `js/levygames-bridge.js` |

הגשר לקוד קלאסי:
```html
<script src="../../js/levygames-bridge.js?v=1"></script>
```
```js
const LG = await window.LevyGames.ready;   // null אם הטעינה נכשלה
if (LG) { const saved = await LG.storage.read('myGame.v1', null); }
```
המשחק חייב לעבוד גם כש-`LG` הוא `null` — בלי שמירה, אבל בלי לקרוס.

### כל קריאות האחסון אסינכרוניות

```js
const saved = await read('myGame.v1', null);
await write('myGame.v1', { v: 1, level: 3 });
```

`await` נדרש גם כשזה נראה מיותר. זה לא סרך עודף: מאחורי השכבה יושב היום
`localStorage` סינכרוני, אבל אם יום אחד יהיה סנכרון בין מכשירים (serverless
+ DB), מחליפים את `js/local-store.js` בלבד — ואף משחק לא ישתנה. API סינכרוני
היה מכריח refactor של כל משחק מחדש.

`await` ברמת המודול עובד ב-`type="module"`, ולכן בדרך כלל אין צורך במבנה
מיוחד. בסקריפט קלאסי — `(async function boot() { ... }())` בסוף הקובץ.

### מה מתוייג-פרופיל ומה גלובלי

| מתוייג-פרופיל (`read`/`write`) | גלובלי (`readGlobal`/`writeGlobal`) |
|---|---|
| התקדמות, שיאים, משחק שמור, משימת היום | השתקת סאונד, רשימת משאלות |

הכלל: **התקדמות שייכת לילדה, העדפה שייכת למכשיר.** מעבר בין פרופילים לא
אמור להפעיל צליל בספרייה.

### שמות namespace

`read`/`write` מקבלים namespace שכולל את גרסת הסכמה של המשחק:
`'chess.v1'`, `'littleDetective.v1'`. הגרסה בשם המפתח ולא בתוך המידע, כדי
שהוולידציה של המשחק תישאר שלו. `write` מוסיף `savedAt` לאובייקטים; שדות
שהמשחק שולח (כולל `v` משלו) נשמרים בלי שינוי.

**הוולידציה היא של המשחק, לא של השכבה.** ראו `games/chess/js/storage.js`.

---

## חוק שני: חוזה ההתקדמות

הפורטל מציג התקדמות על כרטיסי המשחקים בלי להכיר אף משחק ספציפית. הוא
מסתמך על רקורד אחיד שכל משחק מדווח:

```js
await reportProgress('my-game', { kind: 'levels',  done: 7, total: 15 });  // פס התקדמות
await reportProgress('my-game', { kind: 'score',   best: 1240 });          // שיא
await reportProgress('my-game', { kind: 'session', hasSave: true });       // "יש משחק שמור"
```

שלושה כללים מחייבים:

1. **ה-`gameId` חייב להיות זהה ל-`id` ב-`js/games-data.js`.** זה מה שמחבר
   את ההתקדמות לכרטיס. לא תואם = הכרטיס פשוט לא יציג כלום, בשקט.
2. **הדיווח הוא נגזרת של מה שהמשחק כבר שומר — לא מצב חדש.** לקרוא ל-
   `reportProgress()` בדיוק באותה פונקציה שבה המשחק שומר, ומאותם נתונים.
   כל דיווח ממקום אחר יוצר מקור אמת שני, והוא יתנתק.
3. **`total` מגיע מהמשחק**, לא מקובץ הגדרות. אם נוסף שלב — הטוטאל מתעדכן
   לבד.

ב-`js/games-data.js` אפשר להוסיף `progressUnit: 'תיקים'` כדי לשנות את מה
שנכתב ליד הפס (ברירת מחדל: `'שלבים'`).

---

## חוק שלישי: נתיבים

- **בתוך תיקיית משחק — נתיבים יחסיים בלבד:** `../../assets/og.png`,
  `../../js/storage.js`. אף פעם לא נתיב מוחלט. זה מה שמאפשר לאתר לרוץ גם
  בתת-נתיב (GitHub Pages) וגם בשורש דומיין (Vercel/Netlify) בלי שינוי.
- **היוצא מן הכלל היחיד: `404.html`.** הוא מוגש מכל כתובת שגויה שהוקלדה,
  ולכן נתיב יחסי בו ייפתר מול תיקייה שגויה. שם — ורק שם — נתיב מוחלט.
- **תגיות `og:url` / `canonical`** חייבות להיות מוחלטות (דרישת Open Graph).
  להעתיק מדף קיים ולעדכן את הנתיב.
- **case-sensitivity:** שם קובץ/תיקייה בהפניה חייב להתאים בדיוק לאותיות
  הגדולות והקטנות שבדיסק. Windows סולח, שרת האירוח לא. `Images/ship.png`
  ו-`images/ship.png` הם שני דברים שונים בייצור.

### צליל

`js/sfx.js` הוא המקום היחיד שמכיר איפה קבצי הצליל יושבים ואיך הם נקראים.
הוא פותר נתיבים מול עצמו (`import.meta.url`), ולכן עובד מכל עומק:

```js
import { jdSound, localSound, play } from '../../js/sfx.js?v=1';
const tap = jdSound('Cursor - 1', 0.45);   // מחבילת JDSherbert
const win = localSound('level-up.mp3', 0.6); // קובץ בודד ב-assets/
play(tap);
```
בסקריפט קלאסי — `LG.sfx.jdSound(...)` דרך הגשר.

**לא לבנות נתיב לקובץ צליל ביד.** שחמט ו-Space Invaders מוחרגים — לשניהם
מערכת סאונד ייעודית משלהם (ראו `Design.info/sound-mapping.md`).

---

## משחק עם build (React/Vite)

**המקור חייב לשבת בריפו, תחת `src-games/<id>/`.** לא בתיקייה אחות, לא
בתיקיית אב, לא "בצד". מי שמשאיר מקור בחוץ ומעתיק רק את ה-`dist` פנימה
משיג פריסה שעובדת ומקור שלא מגובה בשום מקום.

```
src-games/<id>/     המקור (React + Vite) — עולה לגיט
games/<id>/         תוצר ה-build — עולה לגיט, זה מה שמוגש
```

כרגע שני כאלה: `code-a-bot` ו-`alfa-town`.

`node_modules/` ו-`dist/` של תת-הפרויקט מוחרגים ב-`.gitignore`.
**התוצר שנפרס ל-`games/<id>/` כן עולה לגיט** — הוא מה שהאתר מגיש.

תהליך עדכון:
```bash
cd src-games/<id>
npm run build
rm -rf ../../games/<id>/assets     # שם הבנדל כולל hash - למחוק את הישן
cp -r dist/. ../../games/<id>/
```
בלי המחיקה נשארים בנדלים מתים ב-`games/<id>/assets/` לנצח.

### מלכודת: Vite "מאמץ" קבצים משותפים עם הפורטל

מכיוון שהמקור יושב בתוך הריפו, `../../` בתוך `index.html` נפתר לקבצים
אמיתיים — ו-Vite מעתיק אותם לתיקיית ה-`assets` של המשחק. זה משכפל נכסים,
וגרוע מזה: עותק של `manifest.webmanifest` בתוך `games/<id>/assets/` מקבל
`scope` של התיקייה ההיא (ה-manifest של הפורטל יחסי), ושובר את ה-PWA.

**הפתרון הקיים:** התוסף `portalSharedLinks` ב-`vite.config.ts` מזריק את
תגי ה-`manifest` וה-`apple-touch-icon` ב-`order: 'post'`, אחרי שלב פתרון
הנכסים, כך ש-Vite לא נוגע בהם. כל קישור חדש לקובץ משותף עם הפורטל צריך
לעבור באותה דרך.

תג ה-`<script>` של הגשר (`../../js/levygames-bridge.js`) דווקא עובר בשלום —
Vite רק מזהיר שהוא לא יכול לארוז סקריפט קלאסי, ומשאיר אותו כמו שהוא.

### קוד ב-`Design.info/` — לא

`Design.info/` מוחרג מגיט, ולכן כל קובץ קוד שם **לא מגובה**. סקריפטים
שימושיים (ולידציה, חיתוך נכסים, e2e) הולכים ל-`scripts/` בתוך תת-הפרויקט
ומחווטים ל-`package.json`. ב-`Design.info/` נשארים רק tasks והערות.

---

## חוק רביעי: נכסים — היברידי

| איפה | מה |
|---|---|
| `assets/` בשורש | מה שהפורטל צורך, או שיותר ממשחק אחד צורך: צלילי UI, אייקונים, `og.png` |
| `games/<id>/` | מה שרק המשחק הזה צורך: תמונות, ספרייטים, נתונים |

**אין לרכז נכסי משחקים לתיקייה מרכזית.** תיקיית המשחק היא יחידת הניידות —
אפשר למחוק, להעתיק או לבנות משחק מחדש בלי לגעת בכלום. ריכוז יוצר namespace
גלובלי שדורש תיאום, ומחיקת משחק הופכת לניחוש אילו קבצים נשארו יתומים.

---

## חוק חמישי: Cache-busting

לכל קובץ CSS/JS מצורף `?v=N` — גם בתגיות `<link>`/`<script>` וגם בשורות
ה-`import` בתוך המודולים. **כשמשנים קובץ — מעלים את המספר בכל מקום שמייבא
אותו:**

```bash
grep -rn "?v=" --include=*.html --include=*.js .
```

---

## עברית ו-RTL

- `<html lang="he" dir="rtl">`.
- בעיצוב: `inset-inline-start` ולא `left`, `margin-inline` ולא
  `margin-left/right`.
- **כל הצגה של קלט משתמש — `textContent`, לא `innerHTML`.** שם פרופיל,
  רעיון מרשימת המשאלות. ראו `js/wishlist.js`.
- לוח משחק שחייב להיות משמאל לימין (שחמט) — `direction: ltr` על המכל שלו.

---

## להוסיף משחק חדש

1. תיקייה `games/<id>/` עם `index.html` ו-`instructions.html`.
2. להעתיק מדף משחק קיים: בלוק ה-`<meta>`/Open Graph,
   `<link rel="manifest">`, `apple-touch-icon`.
3. אובייקט אחד ל-`js/games-data.js` — ה-`id` שם הוא ה-`gameId` בדיווח.
4. אחסון דרך `js/storage.js` (או הגשר), ו-`reportProgress()` באותו מקום
   שבו המשחק שומר.
5. להעלות `?v=` בכל מה שנגעת בו.

בדיקה שהכל התחבר: לשחק שלב אחד, לחזור לפורטל, ולראות שההתקדמות מופיעה על
הכרטיס. אם לא — כמעט תמיד ה-`id` לא תואם.

---

## פיתוח מקומי

מודולי ES לא נטענים מ-`file://`, צריך שרת:

```bash
python -m http.server 8000     # מהשורש של LevyGames/
```

זו גם סימולציה של אירוח בשורש דומיין (Vercel/Netlify).

---

## מה שאין כאן, ולמה זה חשוב

אין DB. הכל ב-`localStorage` של הדפדפן, כלומר: אותה ילדה בטאבלט ובלפטופ =
שתי התקדמויות נפרדות; ניקוי היסטוריה מוחק הכל; ו-Safari ב-iOS מוחק
`localStorage` של אתר שלא ביקרו בו 7 ימים.

ופרופילים כאן הם מתג "מי משחקת עכשיו במכשיר הזה" — **לא חשבונות.** אין
סיסמה וכל אחת יכולה לבחור כל פרופיל. זה לא גבול הרשאות.
